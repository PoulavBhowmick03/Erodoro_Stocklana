.anchor
target
node_modules
test-ledger
# Ledger, accounts db and validator keypair written by the local MagicBlock
# stack. Regenerated by `mb-stack --reset`.
magicblock-test-storage
.DS_Store
node_modules
.pnpm-store

# Fundraising strategy and terms — kept local, not in the repo.
docs/funding.md
.next
next-env.d.ts
web/out

# Raw devnet test keypairs. The public halves and the secrets are already in
# web/lib/test-wallets.ts by design; these files are just the source.
.test-wallets/

# Scratch captures from an earlier landing iteration; not an input to anything.
web/.world-shots/
