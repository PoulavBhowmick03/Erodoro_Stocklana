# SPDX-License-Identifier: Apache-2.0
[package]
name = "common"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared math, error table, and pure types for the covered-call protocol."

[lib]
name = "common"

[features]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { workspace = true }

[dev-dependencies]
proptest = "1"
